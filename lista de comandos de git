Comandos en Github
Configurar Nombre: git config --global user.name ""
Configurar Email: git config --global user.email ""
Marco de colores para los comando: git config --global color.ui true
Iniciar GIT en la carpeta donde esta el proyecto: git init
Clonar el repositorio de github o bitbucket: git clone <url>
Añadir todos los archivos para el commit: git add
Hacer el primer commit: git commit -m "Texto que identifique por que se hizo el commit"
Subir al repositorio: git push origin master
Clonar el repositorio de github o bitbucket: git clone <url>
Añadir todos los archivos para el commit: git add
git add <archivo>
git add --all 
git add *.txt
git add docs/*.txt
git add docs/
Cargar en el HEAD los cambios realizados
git commit -m "Texto que identifique por que se hizo el commit"
git commit -a -m "Texto que identifique por que se hizo el commit"
git commit -a 
git commit --amend -m "Texto que identifique por que se hizo el commit"
